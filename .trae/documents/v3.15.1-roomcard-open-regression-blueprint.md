# v3.15.1 云房卡打开回归修复蓝图

**日期**：2026-08-15
**目标**：恢复客户点击后可打开云房卡，不改变宿家接口和房间号标题规则。

## 一、已确认事实

1. 生产 `wecom_kf.log` 已记录原生 `miniprogram` 消息发送成功，但这只能证明企业微信接受了消息，不能证明客户点击后页面可用。
2. 生产只读调用宿家 `byChannelOrder` 返回非空 `appid`、`path` 和合法 `urlLink`；当前 `path` 结构为 `pages/index/index?qrCodeId=...`。
3. `v3.13-roomcard-direct-urllink-blueprint.md` 已记录真实结论：企微原生小程序卡片“发送成功但点击空白”，宿家返回的 `urlLink` 可作为可打开的 B 样式链接卡片。
4. `v3.14-roomcard-native-miniprogram-blueprint.md` 按用户要求把发送方式切回 A，但验收标准只确认了消息送达，未完成点击后页面可用的真实证据闭环。
5. v3.15 的 `chat.html` 安全改动只允许 HTTPS，导致网页富卡片中的合法 `weixin://dl/business/?t=...` 被替换为 `#`。

审计过程中不输出完整订单号、客户标识、appid、pagepath、urlLink、凭据或 access token。

## 二、最小修复方案

### 1. 微信客服房卡

在 `api/wecom_kf.php` 的订单号分支恢复 B 样式：

- `msgtype=link`；
- `link.url` 原样使用宿家 `urlLink`；
- `link.title` 继续使用真实房间号标题；
- `link.desc` 使用宿家分享文案；
- `link.thumb_media_id` 继续使用现有封面素材；
- 发送失败才发送现有文字提示。

不得把 `weixin://` 链接塞入 `miniprogram.pagepath`，也不得本地重写或拼接订单参数。

### 2. 网页富卡片

在 `chat.html` 将 URL 校验拆为：

- 图片只允许 HTTPS；
- 点击链接允许 HTTPS，或严格匹配 `weixin://dl/business/?t=[A-Za-z0-9_-]+`；
- 其他协议仍拒绝。

这样恢复合法微信跳转，同时不重新引入任意协议、脚本协议或未校验外链。

## 三、蓝图审计

### 方案依赖检查

- `sendKfLinkMessage()` 已存在，且 payload 已使用 `url/title/desc/thumb_media_id`。
- `parseRoomCardDelivery()` 已返回宿家原始 `url`、标题、描述和封面。
- 不需要数据库迁移、宿家配合、订单参数改造或小程序源码修改。
- A/B 切换只影响消息发送类型，不影响查询、生成、标题和封面。

### 可验证性

- PHP 可用 `php -l api/wecom_kf.php` 验证。
- 网页协议白名单可用 Node 语法检查和函数行为测试验证。
- 生产通过日志区分 `Sent roomcard link message` 与失败提示发送。
- 本地与 VPS 通过 MD5 校验，PHP-FPM reload 后再做真实微信订单测试。

**蓝图审计结论：通过，可以进入代码修改。**

## 四、验收与回滚

1. 本地 PHP/JS 检查通过。
2. 生产真实输入订单号后收到 B 样式卡片。
3. 点击卡片进入云房卡，不出现空白页。
4. 生产日志不记录完整订单号、客户 ID 或跳转令牌。
5. 若发送失败，文字兜底仍可达。
6. 回滚只恢复 `api/wecom_kf.php` 和 `chat.html` 到部署前版本，不触碰数据库和运行状态文件。

## 五、执行后审计（2026-08-15）

- 本地 `api/wecom_kf.php` PHP 语法检查通过。
- `chat.html` 内嵌 JavaScript 语法检查通过；合法 `weixin://`、HTTPS、`javascript:`、`data:` 行为测试通过。
- 已备份线上旧文件到 `/root/aibisskefu-private-archive/20260815-roomcard-regression/`。
- 已上传并部署：`api/wecom_kf.php`、`chat.html`。
- 本地与生产 MD5 一致：`api/wecom_kf.php`、`chat.html`。
- 生产 `php-fpm` reload 成功且服务为 active；生产 PHP 语法检查通过。
- 未改数据库，未改宿家凭据，未清理微信 cursor、去重缓存或房卡素材状态。

**代码审计结论：通过。** 还需用微信真实输入一次测试订单，确认客户端收到 B 卡片并点击进入云房卡；该项不能用服务器 `send_msg errcode=0` 代替。
