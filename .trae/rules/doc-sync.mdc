---
description: 【AI 铁律】改代码必须同步更新 PRD 与相关文档
globs: *
alwaysApply: true
---

# 【AI 铁律】文档与代码强制同步规则

## 第一条：这是你的义务，无需用户提醒

**每次修改代码后，必须在同一轮对话内同步更新相关文档。** 用户不应再提醒「PRD 同步了吗」——漏同步 = 任务未完成。

主文档：**`PRD_AI_CUSTOMER_SERVICE_v1.0.md`**（宿家客服产品真相源，与代码同级）。

## 第二条：映射表

| 改这个文件 | 必须同步的文档 |
|-----------|----------------|
| `api/chat.php` | `PRD_AI_CUSTOMER_SERVICE_v1.0.md` → §2 路由顺序、§3.3 查单/云房卡、§3.4 增强表、§7 验收、附录 A |
| `api/PromptEngine.php` | `PRD_AI_CUSTOMER_SERVICE_v1.0.md` → §3.4 PromptEngine；`.trae/documents/configurable-agent-blueprint.md` 若动 AgentConfig |
| `chat.html` | `PRD_AI_CUSTOMER_SERVICE_v1.0.md` → §3.5 访客窗、history/rich_content |
| `api/knowledge.php` / `KnowledgeBaseSeed.php` / `templates/**/kb_seed.json` | `PRD_AI_CUSTOMER_SERVICE_v1.0.md` → §3.2 / §3.3.2 KB 政策 |
| `api/RoomQueryFlow.php` / `api/sidecar/**` | `PRD_AI_CUSTOMER_SERVICE_v1.0.md` → §3.3.1 Sidecar；`.trae/documents/room-query-flow-blueprint.md` |
| `api/HandoffTriggers.php` | `PRD_AI_CUSTOMER_SERVICE_v1.0.md` → §3.3.3 转人工 |
| `admin/*.html` | `PRD_AI_CUSTOMER_SERVICE_v1.0.md` → §3.5 后台 |
| `sql/migrations/*.sql` | `PRD_AI_CUSTOMER_SERVICE_v1.0.md` → §5 文件清单；相关 blueprint |
| `scripts/sync-to-mamp.sh` / 部署脚本 | `PRD_AI_CUSTOMER_SERVICE_v1.0.md` → §2.2 部署 |

次要文档（按需）：`.trae/documents/customer-service-plan-v2.md`、各 blueprint。

## 第三条：工作流（与 dev-workflow-soul 一致）

```
用户提需求
    ↓
改工作区代码
    ↓
bash scripts/sync-to-mamp.sh + 测试
    ↓
查映射表 → 更新 PRD（及 blueprint）
    ↓
向用户汇报时说明「PRD 已同步」——若未同步不得声称完成
```

## 第四条：PRD 要更新什么

- **路由/优先级变化** → §2 分流表 + chat.php 处理顺序
- **新函数/新分支** → 对应功能节 + §5 文件清单 + 附录 A（若有规划差异）
- **KB 文案/政策** → §3.3.2 + 验收清单
- **前端交互** → §3.5 + 验收清单
- **bug 修复若改变行为** → 验收项或「已实现增强」表；纯 typo/样式可略

## 第五条：自查清单（提交前）

```
[ ] 改了哪些代码文件？
[ ] PRD 对应章节更新了吗？
[ ] 附录 A / 暂缓项 是否仍准确？
[ ] 用户无需提醒 PRD 同步
```

三条都满足才算完成。
