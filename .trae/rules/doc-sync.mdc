---
description: 【AI 铁律】改代码必须同步更新文档
globs: *
---

# 【AI 铁律】文档与代码强制同步规则

## 第一条：这是你的义务

**每次修改代码后，必须同步更新相关文档。** 这不是可选的，是强制义务。

用户的痛点：以前改代码后文档没人更新，导致文档和代码脱节，AI 一对比就乱套。

解决方案就是**你来做这个同步工作**——用户只管提需求，改代码和更新文档都是你的事。

## 第二条：映射表

| 改这个文件 | 同时更新这篇文档的哪个部分 |
|-----------|--------------------------|
| `api/PromptEngine.php` | `.trae/documents/customer-service-plan-v2.md` → PromptEngine 分层章节 |
| `api/verify.php` | `.trae/documents/customer-service-plan-v2.md` → 验证流程/安全章节 |
| `api/knowledge.php` | `.trae/documents/customer-service-plan-v2.md` → 知识库章节 |
| `api/chat.php` | `.trae/documents/customer-service-plan-v2.md` → API/聊天接口章节 |
| `admin/persona.html` | `.trae/documents/customer-service-plan-v2.md` → 人设配置章节 |
| `admin/knowledge.html` | `.trae/documents/customer-service-plan-v2.md` → 知识库管理章节 |
| `chat.html` | `.trae/documents/customer-service-plan-v2.md` → 客服聊天页面章节 |
| `sql/init.sql` 或 `sql/migrations/*.sql` | `.trae/documents/customer-service-plan-v2.md` → 数据库章节 |
| `deploy.sh` | `.trae/documents/customer-service-plan-v2.md` → 部署章节 |

## 第三条：你的工作流

```
用户提需求
    ↓
你修改代码文件
    ↓
你自动查映射表，找到受影响的文档
    ↓
你同步更新文档内容
    ↓
你在同一个 commit 中包含：【代码改动 + 文档更新】
```

## 第四条：例外（只有这一种情况可以不同步）

只有 **纯样式改动**（CSS 颜色值/间距/字体大小/背景图）可以不更新文档。

除此之外，**任何代码改动都必须同步更新文档**，包括但不限于：
- 新增功能 → 文档新增对应章节
- 修改逻辑 → 文档更新对应描述
- 删除功能 → 文档删除对应章节或标记为已废弃
- 修复 bug → 文档无需大改，但涉及逻辑变化要更新

## 第五条：自查清单

每次提交代码前，过一遍这个清单：

```
[ ] 改了哪些代码文件？
[ ] 查映射表，哪些文档受影响？
[ ] 文档更新了吗？
[ ] 代码和文档在同一个 commit 里吗？
```

三条都满足才能算完成。少一条就是工作没做完。
